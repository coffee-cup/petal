import assert(a: Int, b:Int)

export fn test(): Int {
  let a = 1 + 3
  assert(a, 4)
  let b = a * 2
  assert(b, 8)
  let c = (24 / b) * 123
  assert(c, 369)
  let d = 1 + 2 * (3 - 5) + 6
  assert(d, 3)
  let e = -d
  assert(e, -3)
  return c - e
}

# Results: 372
